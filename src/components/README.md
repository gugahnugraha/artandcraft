# Components Directory

This directory contains shared UI primitives and layouts:
- `ui`: shadcn/ui custom styled primitives (e.g. Button, Input, Card).
- `layouts`: Platform-wide layouts (e.g., Global Header, Global Footer, Sidebar).
