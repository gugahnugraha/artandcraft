# Types Directory

Contains global TypeScript definitions, interfaces, and enums:
- `next-auth.d.ts`: NextAuth custom session/user property overrides.
- `index.ts`: Shared domain entities (e.g., Cart, CheckoutData, ShippingInfo).
